prototype(DMF.BootstrapContent:Panels) < prototype(TYPO3.Neos:Content) {

    headline = ${q(node).property('headline')}
    footer = ${q(node).property('footer')}
    style = ${q(node).property('style')}
    titleElement = ${q(node).property('titleElement')}
    content = TYPO3.Neos:ContentCollection {
		nodePath = 'content'
    }
}